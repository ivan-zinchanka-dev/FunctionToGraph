<Window x:Class="FunctionToGraph.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:scottPlot="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
        xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
        xmlns:local="clr-namespace:FunctionToGraph"
        xmlns:models="clr-namespace:FunctionToGraph.Models"
        mc:Ignorable="d"
        Title="Function to graph" Height="550" Width="1000">
    
    <Window.Resources>
        <models:FunctionGrapher x:Key="function_grapher" />
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.25*" MinWidth="100"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="600"/>
        </Grid.ColumnDefinitions>
        <StackPanel 
            Grid.Column="0" 
            Background="Bisque"
        >
            <StackPanel 
                Orientation="Horizontal"
                Margin="10">
                <TextBlock 
                    Text="y(x)="
                />
                <TextBox
                    x:FieldModifier="private"
                    x:Name="_functionTextBox"
                    Width="150"
                    MaxLines="1"
                    MaxLength="30"
                    
                    TextChanged="OnFunctionTextChanged"
                    
                />
                
                <!--Text = "{Binding Source={ StaticResource function_grapher}, Path=Function, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"-->
                
            </StackPanel>
                
            <TextBlock 
                Text="Enter the function in the field. Use the 'x' to represent an unknown variable. You will see a graph if there are no errors in the notation."
                Margin="10"
                TextWrapping="Wrap"
                TextAlignment="Justify"
            />
            
            <StackPanel 
                Orientation="Horizontal"
                Margin="10">
                <TextBlock 
                    Text="Graph color:"
                />
                <Button
                    Width="100"
                    Margin="10 0 0 0"
                    Background="Red"
                    
                    Click="OnGraphColorButtonClick"
                >
                    
                </Button>
            </StackPanel>
            
            
            
            
            
            
        </StackPanel>
        <GridSplitter
            Grid.Column="1"
            Width="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch"
            ></GridSplitter>
        <scottPlot:WpfPlot
            Grid.Column="2"
            x:Name="_plot" 
            
            />
    </Grid>
</Window>
